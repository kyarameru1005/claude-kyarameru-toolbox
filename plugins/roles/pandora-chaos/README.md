# pandora-chaos

障害を意図的に注入してシステムの耐障害性を検証する実験を設計し、コンテナ隔離環境でのみ安全に注入・観測・自動復旧まで行うときに使う。定常状態・仮説・爆発半径・観測・中断条件の整理と、管理された runner 経由の安全な実行に向いている。

安全は仕組みで担保する: コンテナガード（コンテナ外では実注入を拒否）、時間制限付きの自動ロールバック、opt-in/no-op（実験定義が無ければ何もしない）、dry-run 既定。詳しい使い方は `skills/pandora-chaos/SKILL.md` と `templates/` を参照。
