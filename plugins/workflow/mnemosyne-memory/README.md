# mnemosyne-memory

CLAUDE.md を編集した後にグローバル（`~/.claude/CLAUDE.md`）や上位階層の CLAUDE.md と
突き合わせ、重複・冗長を取り除いてトークンの無駄を減らすスキル。プロジェクト固有の記述だけを
残し、共通ルールはグローバルへ寄せる整理に向いている。

- カテゴリ: `workflow`（skill のみ、対応 agent なし）
- 共通ルールはグローバルに1つ、固有だけを各リポジトリの `CLAUDE.md` に置く方針を支援する。
